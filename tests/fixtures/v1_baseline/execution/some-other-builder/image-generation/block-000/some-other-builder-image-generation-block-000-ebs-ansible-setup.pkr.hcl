# Additional setup for Packer EBS Ansible Builder generated by PackerEbsImageBuilder (some-other-builder)
locals {
    snapshot = var.release ? "" : "-SNAPSHOT"
    image_version = "${var.base_image_version}${local.snapshot}"
}